repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.12.1 # or the latest stable version
    hooks:
      - id: ruff
        args: [--fix] # Automatically fix lint issues where possible
      - id: ruff-format
